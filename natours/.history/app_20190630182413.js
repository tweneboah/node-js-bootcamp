const express = require('express');
const fs = require('fs');
const app = express();



//ROUTING

//Reading files
 const tours = fs

app.get('/api/v1/tours', (req, res) => {
   
})

const port = 3000
app.listen(port, () => {
 console.log(`App is runing on port ${port}`)
})